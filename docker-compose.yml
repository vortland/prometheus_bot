version: '3.0'

volumes:
    prometheus_data: {}
    grafana_data: {}
    alertmanager_data: {}

services:

  alertmanager_bot:
    image: registry.astralnalog.ru/library/prometheus_bot 
    environment:
      - Proxy_Url=none
      - Proxy_Username=none
      - Proxy_Password=none
    volumes:
      - ./config.yaml:/config
      - ./default.tmpl:/config
    command:
      - '/prometheus_bot'
      - '-c=/config/config.yaml'
    restart: always
